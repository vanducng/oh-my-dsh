import omdshPackage from '../../omdsh/package.json'

/** CLI version rendered by the home terminal demo, sourced from the shipped manifest. */
export const omdshVersion: string = omdshPackage.version
